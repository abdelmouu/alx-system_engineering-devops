# Set the default policy to deny all incoming connections
sudo ufw default deny incoming

# Allow incoming SSH connections (port 22/tcp)
sudo ufw allow 22/tcp

# Allow incoming HTTPS connections (port 443/tcp)
sudo ufw allow 443/tcp

# Allow incoming HTTP connections (port 80/tcp)
sudo ufw allow 80/tcp

# Enable the firewall and apply the rules
sudo ufw enable
